export function App() {
  return (
    <main className="app-shell">
      <section className="hero">
        <p className="eyebrow">Gumo</p>
        <h1>Managed game archives, versions, and saves.</h1>
        <p className="copy">
          This scaffold keeps the frontend runnable from the dev shell while the
          backend and API are implemented in later tasks.
        </p>
      </section>
    </main>
  );
}
