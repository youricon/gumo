<UserControl x:Class="Gumo.Playnite.GumoLibrarySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Margin="0,0,0,12">
      <TextBlock Text="Gumo Server URL" Margin="0,0,0,4" />
      <TextBox Text="{Binding GumoServerUrl}" MinWidth="360" />
    </StackPanel>

    <StackPanel Grid.Row="1" Margin="0,0,0,12">
      <TextBlock Text="API Token" Margin="0,0,0,4" />
      <TextBox Text="{Binding ApiToken}" MinWidth="360" />
    </StackPanel>

    <CheckBox Grid.Row="2"
              Content="Import only public Gumo games"
              IsChecked="{Binding ImportPublicGamesOnly}" />

    <TextBlock Grid.Row="3"
               Margin="0,12,0,0"
               TextWrapping="Wrap"
               Foreground="Gray"
               Text="This is an initial scaffold. Sync, upload, install, and save flows are not implemented yet." />
  </Grid>
</UserControl>
